
// la verificacion de los permisos 

export function PermissionVerification(user: string, password: string|number) {
    // recibimos la respuesa del token decodificado 
    //verificar permisos 
    
}